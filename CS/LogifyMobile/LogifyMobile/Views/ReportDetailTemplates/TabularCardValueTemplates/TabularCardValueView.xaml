<?xml version="1.0" encoding="UTF-8"?>
<controls:GridListItem
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Logify.Mobile.Controls"
    x:Class="Logify.Mobile.Views.ReportDetailTemplates.TabularCardValueView"
    BackgroundColor="{DynamicResource GridCellBackgroundColor}"
    Margin="0">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="5"/>
    </Grid.RowDefinitions>
    <BoxView
        Grid.Row="1"
        HeightRequest="1"
        Margin="0,0,0,4"
        HorizontalOptions="Fill"
        VerticalOptions="Start"
        IsVisible="{Binding IsLastCard, Converter={StaticResource invertedBoolConverter}}"
        Color="{DynamicResource GridCellBorderColor}"/>
</controls:GridListItem>
